---
description: Explain how rustio-design bridges the spec to rustio-admin, and the rules
---

Run `cargo run -- explain` and relay the output. Then, if the user asked
something more specific, answer it using `CLAUDE.md` and `README.md` as the
source of truth — especially the single rule: **edit `rustio.design.toml`, run
`rustio-design build`, never edit `generated/`.**
